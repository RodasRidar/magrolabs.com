import { SinceDatePipe } from './since-date.pipe';

describe('SinceDatePipe', () => {
  it('create an instance', () => {
    const pipe = new SinceDatePipe();
    expect(pipe).toBeTruthy();
  });
});
